#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num = 48;
while (num < 58)
{
if (num == 57)
{
putchar(num);
}
else
{
putchar(num);
putchar(',');
putchar(' ');
}
num++;
}
return (0);
}
