#include <string.h>
#include "main.h"

/**
 * _strlen - is a function that returns the length of a string.
 *
 * @s: the Argument of the function
 *
 * Return:  returns the length of a string (Success).
 */
int _strlen(char *s)
{
return (strlen(s));
}
