#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - is a function thats prints the alphabet,
 * in lowercase, followed by a new line
*/
int _islower(int c)
{
if (c > 96 && c < 123)
{
return (1);
}
else
{
return (0);
}
}
