#include <stdio.h>
#include "main.h"

/**
 * _abs - is a function thats returns the absolute value of an integer
 *
 *@n: the Argument of the function
 *
 * Return: return the absolute value (Success)
*/

int _abs(int n)
{
  return (abs(n));
}
