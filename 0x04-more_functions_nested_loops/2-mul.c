#include <stdio.h>
#include "main.h"

/**
 * mul - is a function that multiplies two integers.
 *
 *@a: the first Argument of the function
 *@b: the second Argument of the function
 *
 * Return: return the multiplication of two numbers (Success)
*/

int mul(int a, int b)
{
return (a * b);
}
