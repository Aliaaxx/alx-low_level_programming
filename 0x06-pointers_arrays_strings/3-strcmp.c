#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcmp - is a function that compares two strings.
 *
 * @s1: the first Argument of the function
 * @s2: the second Argument of the function
 *
 * Return: Return value(Suceess)
 */
int _strcmp(char *s1, char *s2)
{
return (strcmp(s1, s2));
}
