#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "main.h"

/**
 * _atoi - is a function that convert a string to an integer.
 *
 * @s: the Argument of the function
 *
 * Return: integers and 0 if no numbers (Success)
 */
int _atoi(char *s)
{
return (atoi(s));
}
